# ============================================================================
# STREAMWARD AI ASSISTANT - FRONTEND ENVIRONMENT CONFIGURATION
# ============================================================================
# This file contains all environment variables for the Next.js frontend.
# Copy this to .env.local and fill in your actual values.
#
# Organization:
# 1. NextAuth Configuration (authentication framework)
# 2. Okta Configuration (authentication provider)
# 3. API Configuration (backend connectivity)
# ============================================================================

# ============================================================================
# SECTION 1: NEXTAUTH CONFIGURATION
# ============================================================================
# Required NextAuth settings for session management and authentication

# NextAuth URL (must match your frontend deployment URL)
# Local development: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL=http://localhost:3000

# NextAuth secret key (for encrypting session tokens)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key

# ============================================================================
# SECTION 2: OKTA AUTHENTICATION CONFIGURATION
# ============================================================================
# Okta provider credentials and configuration for NextAuth

# Okta app credentials (from Okta console)
# Grant types required: Authorization Code + Refresh Token
OKTA_CLIENT_ID=your-okta-client-id
OKTA_CLIENT_SECRET=your-okta-client-secret

# Okta issuer URL (your org domain)
# Example: https://yourorg.okta.com
OKTA_ISSUER=https://your-okta-domain.okta.com

# Okta organization base URL (for logout and public endpoints)
NEXT_PUBLIC_OKTA_BASE_URL=https://your-okta-domain.okta.com

# Okta client ID (same as OKTA_CLIENT_ID, but public for browser access)
# Required for custom server OAuth flow (A2A token exchange)
NEXT_PUBLIC_OKTA_CLIENT_ID=your-okta-client-id

# Okta authorization server ID for custom server OAuth flow (A2A token exchange)
# This is used for multi-agent workflows with custom authorization server
# Get this from your backend OKTA_MAIN_SERVER_ID configuration
NEXT_PUBLIC_OKTA_MAIN_SERVER_ID=your-okta-authorization-server-id

# ============================================================================
# SECTION 3: API CONFIGURATION
# ============================================================================
# Backend API connectivity

# Backend API base URL (for chat, documents, and other API calls)
# Local development: http://localhost:8000
# Production: https://api.your-domain.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# WebSocket URL (for real-time streaming from backend)
# Local development: ws://localhost:8000
# Production: wss://api.your-domain.com
NEXT_PUBLIC_WS_URL=ws://localhost:8000
